name: Release & Deploy Browser Extensions

permissions:
  contents: write

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      submit_firefox:
        description: 'Submit to Firefox Add-ons (AMO)'
        required: true
        type: boolean
        default: true

jobs:
  build-and-deploy:
    name: Build, Verify & Publish
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Root Dependencies
        run: npm ci

      - name: Install Extension Dependencies
        run: npm --prefix extension ci

      - name: Run Extension Unit Tests
        run: npm run test:ext

      - name: Build & Package Chrome and Firefox Extensions
        run: npm run package:all

      - name: Upload Release Artifacts to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            extension/.output/releases/v*/*.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Submit to Mozilla Add-ons (Firefox)
        if: startsWith(github.ref, 'refs/tags/v') || github.event.inputs.submit_firefox == 'true'
        working-directory: ./extension
        run: npx wxt submit
        env:
          FIREFOX_EXTENSION_ID: ${{ secrets.FIREFOX_EXTENSION_ID || 'notion-job-scraper@lij0825.com' }}
          FIREFOX_JWT_ISSUER: ${{ secrets.FIREFOX_JWT_ISSUER }}
          FIREFOX_JWT_SECRET: ${{ secrets.FIREFOX_JWT_SECRET }}
